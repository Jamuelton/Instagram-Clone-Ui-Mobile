import { StyleSheet } from "react-native";

const containerStyle = StyleSheet.create({
  container: {
    height: "100%",
  },
  topView: { flex: 1, marginTop: 10 },
  storyView: { flex: 2 },
  postView: { flex: 11 },
  bottomView: { flex: 0.5 },
});

export default containerStyle;
